import Placeholder from "@/components/layout/placeholder";

export const metadata = { title: "Get in touch — ClimbX Digital" };

export default function Page() {
  return <Placeholder eyebrow="Connect" title="Get in touch" />;
}
